/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/

import React from 'react';

// FIX: Stubbed out the Auth component. As per guidelines, API keys must come from environment variables,
// and there should be no UI for entering them. This component is obsolete and was causing a build error
// due to the removal of the 'User' type.
const Auth: React.FC = () => {
  return null;
};

export default Auth;
